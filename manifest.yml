---
applications:
  - name: tuts
    memory: 256M
    instances: 2
    path: .
    command: bin/rake cf:on_first_instance db:migrate && bin/rails server -p $PORT
    services:
      - rails-postgres

